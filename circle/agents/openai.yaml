interface:
  display_name: "Circle"
  short_description: "Manage local projects, issues, and dependency DAGs"
  default_prompt: "Use $circle to import and manage this project."
policy:
  allow_implicit_invocation: false
